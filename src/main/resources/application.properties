# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9hf72rtqb8s739fhecg-a/opac_db_k7f0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:opac_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:265zH6CIGl3VhjCAWPSOWaout5k8sXQw}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Server
server.port=${PORT:8081}

# Logging
logging.level.org.springframework=INFO
logging.level.com.example.opac=DEBUG